package org.cswteams.ms3.dto.logout;

public class LogoutRequestDTO {
    // NOT technically necessary: the data (JWT) is NOT in the body, but in the Authorization header,
    // so there is nothing to serialize from the body
}
