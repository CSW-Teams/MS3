package org.cswteams.ms3.control.utils;

public class MappaTurni {
/*
    public static Shift turnoDTOToEntity(RotationDTO dto) throws ShiftException {
        Shift shift = new Shift(dto.getOraInizio(),dto.getDurata(), MappaServizio.servizioDTOtoEntity(dto.getServizio()), dto.getMansione(), dto.getTimeSlot(), dto.getRuoliNumero(),dto.isReperibilitaAttiva());
        shift.setBannedConditions(dto.getBannedConditions());
        return shift;
    }

    public static RotationDTO turnoEntityToDTO(Shift entity){
        RotationDTO dto = new RotationDTO(entity.getId(), entity.getTimeSlot(),entity.getStartTime(), entity.getDuration(), MappaServizio.servizioEntitytoDTO(entity.getServizio()), entity.getMansione(), entity.isAvailability(), entity.getQuantityShiftSeniority());
        dto.setBannedConditions(entity.getBannedConditions());
        return dto;
    }


    public static Set<RotationDTO> turnoEntityToDTO(List<Shift> turni){
        Set<RotationDTO> turniDTO = new HashSet<>();
        for(Shift shift : turni){
            turniDTO.add(turnoEntityToDTO(shift));
        }
        return turniDTO;
    }*/

}
