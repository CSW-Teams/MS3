# Pulls in container datasource placeholders so SchemasInitializer runs during tests
spring.profiles.include=container

# Hibernate schema creation stays disabled; the initializer creates tables and 2FA columns per tenant
spring.jpa.hibernate.ddl-auto=none
